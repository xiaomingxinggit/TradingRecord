# 项目协作规范

## 每次接手

- 用户当前指令优先。先读本文件、`TASKS.md` 的当前任务卡，再按需读 `ARCHITECTURE.md` 对应模块；不要默认重读完整聊天、README 或全仓库。
- 在 `git rev-parse --show-toplevel` 返回的 TradingRecord 根目录工作。原 TradingRecord-public 已归档，不再双目录开发、复制发布或维护两套源码。
- 先看 `git status --short` 和当前任务相关 diff，记录已有未提交文件的归属。只改自己负责的文件；不覆盖、暂存、提交其他任务的改动。
- `TASKS.md` 是进度和交接的唯一清单，`ARCHITECTURE.md` 是模块边界和关键决策索引；README 继续面向使用者。发现偏差时修正对应来源，避免复制多套说明。

## 角色与调度

- PM / 架构负责人：复杂任务使用 `gpt-6-astra`，先定目标、边界、数据约束和验收条件，再在任务卡中拆解依赖。简单文案和样式不必增加架构环节。
- DEV / 持续调度者：默认 `gpt-5.6-luna`、medium，负责进度、依赖、简明上下文、简单实现、集成和验证记录。能力不足时升级，不用反复试错掩盖难度。
- 涉及数据迁移、MT5 解析算法、跨模块一致性或复杂并发时，请委派独立 Astra Worker；先由 PM 确定设计。仅传任务卡、必要文件、接口和验收条件；支持时使用 `fork_turns="none"`，不要默认继承全部历史。
- 同一问题在一次局部修复后仍失败，或发现架构/数据约束冲突，DEV 带最小复现证据与已试方案升级给 PM。设计变更先更新架构决策和任务卡，再续做。
- 只有能独立推进且文件所有权互不重叠的任务才并行；有依赖则顺序执行。共享入口、依赖清单与锁文件由单一集成人修改。小任务直接完成，不额外建代理做相同审查。
- 模型由实际运行工具或用户设置选择；本文不会自动切换模型。调度工具支持覆盖时显式传 model/thinking，记录请求值、是否被接受及失败原因。不可用就报告限制，不谎报已切换，也不擅自改全局配置。
- 沿用现有 DEV 任务承接工作，不默认新建用户任务或后台定时器。长期角色表示持续负责，并非让代理空转等待。

## 最小交接

派工包含：任务 ID、目标、依赖、允许改动文件、禁止触碰内容、相关决策、验收/验证方式、提交与推送范围。

Worker 只返回：完成内容、文件列表、验证命令与结果、未解决问题、提交号（如有）。默认不超过 10 条要点；日志仅附定位错误所需的片段。

DEV 负责更新任务状态并集成一次。已有有效验证结果可复用；只有后续修改、失败或明确遗漏才重跑/追加审查，不让 PM 再完整重写或重复审查相同工作。

## 验证与交付

- 当前用户要求：不新增或运行测试，不做 API 试跑或浏览器验收。必要的 `npm run build`（包含 TypeScript 检查）允许执行；须明确它不等于运行时验证。用户改变要求后同步任务卡。
- 文档任务核对文件路径、规范一致性和 diff 即可，不启动服务或构建产品。代码任务按改动需要执行构建、静态检查；不能因为省额度而省略已约定检查。
- 如现有验证方式无法覆盖关键风险，记录具体未验证行为和原因；不伪称通过，不把未经授权的测试隐藏为“检查”。
- 完成已授权工作后仅提交本任务文件。推送需有明确授权；提交与推送分别报告，不默认用前一次发布授权覆盖后续任务。
- 暂存前检查差异和敏感内容，使用明确文件路径，不使用 `git add .` 或 `git add -A` 混入其他任务文件。

## 数据和产品边界

- 本项目只记录，不连接交易账户或执行交易。优先复用 Element Plus，保持表单简洁、日夜主题一致。
- `data/`、数据库及 WAL/SHM、真实截图、MT5 报告、私人历史归档、凭据不得提交。忽略规则不是脱敏的替代品；文档示例仅用虚构数据。
- 使用当前脱敏 Git 历史；不把旧私人 `.git`、objects、refs 或备份重新接入公开仓库。备份位置只写本机忽略文件。
- 数据迁移先明确兼容与回退方案，并取得一致性备份；不删除旧表/历史隐藏字段，不用新建空库解决问题。
- 不为排查文档或 UI 打开并修改真实数据库。模块不变量见 `ARCHITECTURE.md`。

## 成本与依据

优化目标是减少重复上下文、返工和无效调度。按实际可得数据记录调用、升级、重复验证与结果；没有计量时不承诺节省百分比或缓存折扣。模型分工是项目策略，效果须随任务评估。

模型设置和独立代理的能力参考 [官方 Subagents 文档](https://learn.chatgpt.com/docs/agent-configuration/subagents)；具体可用模型和参数以当前工具返回为准。
