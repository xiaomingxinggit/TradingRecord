# TradeLog AI 接手手册

本文件是仓库内 AI 与开发代理的长期项目上下文。先读本文件，再按任务打开相关源码；不要依赖聊天历史、临时任务说明或某次提交号理解项目。面向使用者的介绍与启动说明维护在 `README.md`。

## 1. 强制项目要求

- 以用户当前需求为准。项目只负责记录交易，使用 Element Plus，保持表单简洁及日夜主题一致。
- 只在当前 `TradingRecord` 仓库开发，不维护另一套发布源码。
- 保留其他任务的未提交改动，不覆盖或混入本次提交。
- 不新增或运行测试，不做 API、OCR 试跑或浏览器验收。必要的构建和静态检查允许执行，结果如实说明。
- 不为开发验证读取或修改真实数据库；不删除旧数据、截图或历史表。确需数据迁移时先做好一致性备份与回退方案。
- 数据库及 WAL/SHM、真实截图、交易报告、凭据和私人 Git 历史不得提交或上传。
- 完成修改后仅提交本次相关文件；远程推送须有用户明确授权。
- 产品使用说明维护在 `README.md`。
- 导出功能维持当前行为，暂停新增或调整；除非用户之后明确恢复，不开展导出功能开发。

## 2. 产品定位

TradeLog 是本地优先的个人交易记录工具，核心对象是“交易计划”。它帮助用户在交易前记录判断，在计划执行后关联实际订单，并通过事件和复盘回看计划的完整过程。

明确边界：

- 只记录，不连接 MT5 或其他交易账户，不读取账户，不下单，也不修改真实仓位。
- 不在应用内提供 K 线回放或交易执行能力。
- OCR 只解析用户主动提供的 MT5 截图，结果是待人工核对的草稿，不能被当作交易事实自动执行。
- 数据默认只保存在本机；服务和开发服务器均绑定或代理到本机地址。
- 导出的 ZIP 是人类可读资料，不是数据库迁移或恢复格式。

## 3. 当前用户流程

工作空间默认进入“数据概览”，导航顺序为“数据概览 → 行情吐槽 → 交易计划”。看板只读汇总本机已保存计划、关联订单、事件和复盘；进入时读取，也可手动刷新。近期记录支持进入原有计划详情，离开计划去往其他工作空间板块必须复用计划、订单草稿与复盘的离开保护。行情吐槽是独立于计划的本地个人图文时间线，不纳入数据概览的计划统计。

1. 计划列表默认展示今天创建的计划，按创建时间展示并可切换升降序；可按日期范围或“今天、昨天、上周、上月”与计划状态组合筛选。
2. 新建计划时只显示交易计划表单。首次保存后才出现“交易计划 / 关联订单 / 计划事件 / 计划复盘”四个板块。
3. 用户填写计划内容、上传或粘贴行情截图，并保存为草稿或待触发。
4. 已保存计划可以录入关联订单；一个计划可关联多笔订单，一笔订单最多属于一个计划。
5. 用户可手工修改未锁定订单状态，或用“更新订单”识别同一订单的新截图并确认关键字段变化；已平仓订单核对完成后可永久锁定。
6. 订单写入产生只读计划事件。用户最后在计划复盘中记录结论、纪律、总结与下次行动。
7. “导出数据”把所有已保存计划整理为 Markdown 和图片 ZIP。

所有未保存的计划、订单识别草稿、订单更新草稿、复盘输入和未发布的行情吐槽都应受离开保护。保存进行中时禁止造成竞态的页面切换或重复写入。

## 4. 核心模型与不变量

### 4.1 交易计划

当前字段包括：品种、方向、分析周期、市场状态、关键结构、入场理由、出场理由、计划入场价、止损价、止盈价、状态、状态变更时间和取消原因。

- 品种默认 `XAUUSD`，允许从候选项搜索或输入自定义代码。
- 方向为未填写、`buy`（做多）或 `sell`（做空）。
- 周期为未填写或 `M1 / M5 / M15 / M30 / H1 / H4 / D1 / W1 / MN1`。
- 市场状态为 `uptrend / downtrend / range / uncertain`。
- 入场理由最多 5000 字，出场理由最多 500 字，关键结构最多 300 字。
- 三个计划价位均可留空；填写时必须大于 0，并符合方向关系。做多要求止损 < 入场 < 止盈，做空相反。三项完整时前端展示按价格距离计算的 R 倍数，不计交易成本。
- 每份计划最多 4 张行情截图，每张不超过 5 MB，仅接受签名与声明 MIME 一致的 PNG、JPEG、WebP。行情截图作为 BLOB 随计划保存在数据库中。
- 编辑计划正文必须保留数据库中的最新状态，不能由旧页面覆盖状态变化。
- 创建时间不可因编辑改变；更新内容或状态时才改变更新时间。

计划状态：

| 值 | 界面名称 | 约束 |
| --- | --- | --- |
| `draft` | 草稿 | 内容可不完整 |
| `ready` | 待触发 | 必须有品种、方向、分析周期和入场理由 |
| `executed` | 已执行 | 必须有品种、方向、分析周期和入场理由 |
| `untriggered` | 未触发 | 手工标记 |
| `abandoned` | 取消 | 可填写取消原因，切换离开后保留历史原因 |
| `expired` | 失效 | 手工标记 |

新计划只允许先保存为 `draft` 或 `ready`。状态变更单独记录 `statusChangedAt`；状态标签可在列表和详情中操作。

### 4.2 关联订单

订单是独立数据实体，订单号 `ticket` 以十进制字符串保存并全局唯一。当前产品没有独立订单管理页，但底层订单不能依附为计划 JSON 的一部分。

- 关系：一个计划可关联多笔订单；一笔订单最多关联一个计划。
- 同一订单号已经属于当前计划时，不重复创建；属于其他计划时返回冲突，绝不自动转移。
- 方向为 `buy / sell`。
- 状态仅为 `pending`（挂单）、`open`（持仓中）、`closed`（已平仓）。人工改状态只改状态，不清空、不推导其他字段。
- 可记录手数、挂单/开仓/平仓时间和价格、截图止损、截图止盈、截图盈利。未知值保持 `null`，不得用猜测或零填充。
- MT5 报告时间按 `YYYY.MM.DD HH:mm:ss` 原样保存，不做时区转换。
- 手数和所有价格允许小数；除截图盈利可为负数外，已填写的数值必须大于 0。
- 订单列表展示订单号、品种、方向、手数、开仓价、止损、止盈、盈亏比、当前状态、锁定状态和更新时间。
- 盈亏比不持久化，按当前 `abs(reportedTP - openPrice) / abs(openPrice - reportedSL)` 计算止盈距离 / 止损距离，显示如 `2.35 : 1`，不计成本、不代表实际盈利。三个价格必须是大于 0 的有限数，止损距离必须大于 0；止损移动到开仓价另一侧后仍按当前距离计算，目标价与开仓价相同时可显示 `0.00 : 1`。无效字段或风险为零时显示 `—`，不能出现 NaN/Infinity。列表与导出语义一致。
- 只有 `closed` 订单可锁定，记录 `locked_at`（API `lockedAt`）ISO 时间，旧订单为 `null`。锁定不可撤销，不提供解锁。后端必须以 409 拒绝锁定订单的状态或 compared-fields 修改；前端禁用状态菜单并拒绝该订单的截图对比更新，读取与导出仍可用。
- 锁定请求仅接受 `expectedUpdatedAt`，在事务内校验归属、乐观并发、已平仓状态，再同时写入锁定时间、更新时间和事件。重复锁定可返回当前订单 `changed=false`，不产生额外事件。

### 4.3 计划事件

计划事件是追加式、只读审计记录，目前只有：

- `order_created`：创建订单或把尚未关联的订单关联到计划。
- `order_status_changed`：订单状态实际发生变化。
- `order_fields_changed`：通过更新订单流程确认手数、止损或止盈发生变化，事件同时记录必填的修改原因、当时情绪和服务端固定的 `source: 'ocr'`；或记录锁定的 `lockedAt: null → ISO 时间`。锁定字段显示为“锁定状态/订单已锁定”，不新增事件类型或改动事件表 CHECK 约束。

事件和对应订单写入必须在同一个 SQLite 事务中成功或回滚。仅查看数据不产生事件，不为旧数据补造历史。事件不可编辑或删除，按发生时间倒序展示和导出。

### 4.4 计划复盘

每个计划最多一份复盘，后续保存更新同一行：

- 计划结论 `assessment`：未填写、`valid / partial / invalid / unverified`。
- 执行纪律 `discipline`：未填写、`followed / deviated / broken / not_executed`。
- 复盘总结最多 3000 字。
- 下次行动最多 2000 字。

四项可分别留空，但保存时至少填写一项。写入携带 `expectedUpdatedAt` 做乐观并发检查；旧页面不能覆盖更新版本。未保存内容在切换板块或离开计划时必须提示。

### 4.5 行情吐槽

- 独立、追加式的本地个人时间线，按 `createdAt` ISO 时间倒序显示，同一时间按写入顺序倒序排列；不依附交易计划，不引入账号、评论、点赞。
- 正文最多 2000 字，保存时去除首尾空白。文字或图片至少一项；允许纯文字、纯图片、图文，不提供编辑或删除。
- 每条最多 4 张、单张不超过 5 MB，仅签名与声明 MIME 一致的 PNG/JPEG/WebP；复用 `server/images.mjs` 校验，正文与图片 BLOB 必须在同一个 SQLite 事务内写入。
- 列表只读取文字、时间与图片元数据，不读取 BLOB；图片按需从独立 API 加载，返回正确 MIME、长度和 `nosniff`，不使用文件名拼接路径。
- 支持选图、粘贴、缩略图预览和发布前移除；卸载或移除时释放临时 URL，粘贴监听仅在行情吐槽视图挂载期间存在。
- 工作空间切换、刷新和关闭页面须保护未发布内容。发布时阻止重复提交和离开，失败保留草稿；时间线刷新不得清空草稿。
- 不纳入现有数据概览统计或交易计划 ZIP；第一版读取全部文字/图片元数据，无分页。

## 5. OCR 与订单更新流程

订单 OCR 由 `server/order-ocr.mjs` 在本机执行，使用 Sharp 预处理和 Tesseract.js 识别。它针对用户提供的完整宽度 MT5 固定表格布局，支持浅色表格以及整行蓝色选中的单行截图，不支持任意深色主题，也不是通用表格识别器。蓝色选中行须先按整行颜色覆盖率、横向分布和单行比例检测并归一化为白底深色字，再复用三种布局识别；不要仅按整体亮度反色普通行或局部彩色单元格。

支持三种候选布局：挂单、持仓中、已平仓。挂单和持仓共享列网格，再通过订单类型、时间、末列结构等证据区分；已平仓使用独立的双时间、双价格布局。表头、汇总行和空白区域应跳过。当前后端最多返回 20 行候选，但产品交互以单张、单行截图核对为主。

导入流程：

1. 前端以 `image` 字段将一张不超过 5 MB 的 PNG/JPEG/WebP 发给 `/api/order-ocr`。
2. 服务比较三种布局并返回检测状态、候选行、置信信息和警告。
3. 前端显示可编辑草稿和原图预览。低置信度或未知字段留空，由用户核对补充。
4. 用户确认单行后才写入订单并关联当前计划。
5. 订单截图只存在于当前浏览器预览和请求内存中，不写入数据库，也不随订单保存。

“更新订单”流程：

1. 只接受当前计划中已存在且未锁定订单的截图，以订单号精确匹配；匹配已锁定订单时明确报错。
2. 当前只比较 `volume / reportedSL / reportedTP`，其他识别字段不参与更新。
3. 未识别值不能清空原值，只展示实际差异，用户确认后才写入。
4. 发现实际差异后，修改原因必填且去除首尾空白后最多 500 字，当时情绪必须是 `calm / confident / hesitant / nervous / fearful / greedy / impulsive` 之一。
5. 请求带订单 `expectedUpdatedAt`；发生并发变化时返回冲突，必须重新加载并再次识别。
6. 确认写入后追加 `order_fields_changed` 事件，订单字段更新与事件写入保持同一事务。

不要把 OCR 置信度、原始识别文本或截图写入正式订单，除非用户明确改变产品模型并设计迁移方案。

## 6. 架构与本地存储

应用是单仓库本地 Web 应用：

- 前端：Vue 3 + TypeScript + Element Plus + Vite。
- 后端：Express 5，ES modules。
- 存储：Node.js 内置 `node:sqlite` 的同步 API，数据库为 `data/trading.sqlite`，启用 WAL 和外键。
- 图片识别：Sharp + Tesseract.js，全部本机处理。
- 导出：JSZip 生成 `交易计划.zip`。

开发时 Vite 使用 `127.0.0.1:5173`，把 `/api` 代理到 `127.0.0.1:3001`。构建后 Express 同时提供 `dist` 静态文件和 API，只监听 `127.0.0.1`。API 如果带 `Origin`，只接受 localhost、127.0.0.1 或 `[::1]`。

当前表：

| 表 | 用途 |
| --- | --- |
| `plans` | 计划主体；当前计划字段存放在 `payload` JSON 中，旧 `account_id` 列保留但当前写入清空 |
| `plan_images` | 行情截图 BLOB，外键关联计划 |
| `tr_orders_v2` | 独立订单及计划关联，订单号唯一 |
| `tr_plan_events_v1` | 追加式订单事件 |
| `tr_plan_reviews_v1` | 每个计划一行的最新复盘 |
| `market_rants_v1` | 独立行情吐槽正文与创建时间，只追加 |
| `market_rant_images_v1` | 吐槽图片 BLOB 与文件名、MIME、大小、顺序，外键关联吐槽 |

初始化使用 `CREATE TABLE/INDEX IF NOT EXISTS`，订单锁定采用增量迁移：初始化 `tr_orders_v2` 后查询列元信息，仅缺少 `locked_at` 时执行 `ALTER TABLE tr_orders_v2 ADD COLUMN locked_at TEXT`，旧行自然为 NULL，不删除或重建表。首次启动新版本前须停服并完整备份 `data/`（含可能存在的 WAL/SHM），见第 8 节。旧版本忽略新 nullable 列，数据结构兼容但不执行锁定约束；保留锁定保护须使用新版本。恢复升级前状态时，停服并保留升级后目录，再用完整备份恢复整个 `data/`；旧备份不包含升级后的新记录。不要为了重构删除旧表、旧字段、历史截图或未知数据，也不要通过新建空库绕过兼容问题。其他 schema 变化同样须先明确兼容、备份与回退路径。

行情吐槽升级仅使用 `CREATE TABLE/INDEX IF NOT EXISTS` 创建上述两张独立表与索引，不修改旧表或旧数据。首次启动前按第 8 节停服并完整备份已有 `data/`。旧版本忽略新表且不能查看吐槽；保留升级后完整数据目录供新版本再次读取。若恢复升级前备份，先停服并保留升级后目录，再恢复整个 `data/`，旧备份不包含升级后发布的吐槽。开发验证不得启动服务来应用 schema，亦不得打开真实数据库。

当前 API 边界：

| 路径 | 用途 |
| --- | --- |
| `GET /api/overview` | 在同一只读快照内汇总全量计划、关联订单、复盘、事件及近期记录，不读取截图 BLOB |
| `GET/POST /api/market-rants` | 查询时间线或发布；multipart 只接受一个 `content` 字段和最多 4 个 `images` 文件 |
| `GET /api/market-rants/:rantId/images/:imageId` | 按吐槽归属读取图片 |
| `GET /api/plans`、`GET /api/plans/:id` | 计划列表与单份计划 |
| `POST /api/plans`、`PUT /api/plans/:id` | multipart 保存计划和行情截图 |
| `PATCH /api/plans/:id/status` | 单独修改计划状态 |
| `GET /api/plans/:id/images/:imageId` | 读取已保存行情截图 |
| `GET /api/plans/export` | 导出全部已保存资料 |
| `POST /api/order-ocr` | 在内存中识别单张订单截图 |
| `GET/POST /api/plans/:planId/orders` | 查询或创建并关联订单 |
| `PATCH /api/plans/:planId/orders/:orderId/status` | 只修改订单状态 |
| `PATCH /api/plans/:planId/orders/:orderId/compared-fields` | 确认截图对比后的关键字段变化 |
| `PATCH /api/plans/:planId/orders/:orderId/lock` | 携带 expectedUpdatedAt 永久锁定已平仓订单 |
| `GET /api/plans/:planId/events` | 查询只读计划事件 |
| `GET/PUT /api/plans/:planId/review` | 读取或保存计划复盘 |

当前没有删除计划、删除订单、解除订单关联或独立订单管理 API。不要在界面上暗示这些能力已经存在。

## 7. 目录与文件地图

### 根目录

- `README.md`：面向使用者的项目主页、启动和数据说明。
- `AGENTS.md`：本 AI 接手手册和长期工程约束。
- `package.json`：运行脚本、Node 版本及依赖。
- `vite.config.ts`：Vite 与本机 API 代理。
- `src/plans.css`：计划列表、表单和详情的主要布局样式。

### 前端 `src/`

- `App.vue`：应用外壳、导航、主题、帮助和导出入口。
- `components/DataOverview.vue`：数据概览指标、状态分布、品种排行、复盘覆盖率、近期记录与刷新/错误/空态。
- `components/MarketRants.vue`：行情吐槽发布、选图/粘贴、图片预览、时间线与未发布草稿离开保护。
- `components/OpeningPlans.vue`：计划列表、新建/查看/编辑、状态、日期筛选、板块协调及离开保护，是前端核心编排组件。
- `components/PlanOrders.vue`：订单导入、OCR 草稿、更新订单、关联订单列表和订单状态操作。
- `components/PlanEvents.vue`：只读计划事件时间线。
- `components/PlanReview.vue`：复盘读取、保存、并发控制和离开保护。
- `components/PlanStatusMenu.vue`：计划状态标签菜单。
- `components/SymbolSelect.vue`：可筛选和自定义的品种选择。
- `components/AppNavigation.vue`、`HelpDialog.vue`：导航与产品内说明。
- `orders.ts`、`plan-status.ts`、`symbols.ts`：前端领域类型、标签与候选数据。
- `ui/theme.ts`、`ui/theme.css`、`ui/element-styles.ts`：日夜主题、全局 UI 规则和 Element Plus 按需样式入口。

### 后端 `server/`

- `index.mjs`：Express 应用、API 挂载、本机来源限制和生产静态文件服务。
- `store.mjs`：SQLite 打开、WAL/外键设置及各存储初始化。
- `overview.mjs`：只读统计服务；不变更表结构、不持久化派生指标。在同一读事务中汇总全部计划、关联现有计划的订单和事件；复盘至少一项非空才计数，覆盖率以全部计划为分母。品种忽略首尾空格并按大写归类，展示前 5；近期 6 份计划按计划/订单/事件/复盘的最新时间排列。
- `plans.mjs`：计划和行情截图校验、持久化、状态与导出快照。
- `market-rants.mjs`：独立吐槽与图片表初始化、事务保存、元数据时间线、图片读取与 multipart 路由。
- `orders.mjs`、`plan-orders.mjs`：订单约束、事务、计划关系和路由。
- `order-ocr.mjs`、`order-ocr-route.mjs`：三类 MT5 布局识别和单图上传入口。
- `plan-events.mjs`、`plan-events-router.mjs`：追加式事件存储和查询。
- `plan-reviews.mjs`、`plan-reviews-router.mjs`：每计划一份复盘及乐观并发控制。
- `images.mjs`：图片数量、大小、签名和文件名校验。
- `export.mjs`：数据库快照转 Markdown 与图片 ZIP。

## 8. 导出与备份语义

导出覆盖全部已保存计划，不受当前列表筛选和分页影响。ZIP 内含 `交易计划.md` 和稳定命名的 `images/` 路径，计划按创建时间倒序；每份计划包含当前计划字段、行情截图、关联订单、计划事件和最新复盘。

导出不包含：行情吐槽及其图片、未保存的计划或复盘输入、OCR/更新订单草稿、订单 OCR 原图、识别元数据。用户应完整解压 ZIP 后阅读 Markdown；ZIP 不提供导入功能，也不能替代 SQLite 备份。

正确备份数据库：先停止所有使用数据库的进程，再复制整个 `data/` 目录，包括可能存在的 `trading.sqlite-wal` 和 `trading.sqlite-shm`。不得在运行中只复制主数据库文件并声称备份完整。

## 9. UI 与交互约定

- 优先使用现有 Element Plus 组件和 `src/ui/element-styles.ts` 的按需样式入口。新增组件时同步确认其官方 CSS 已导入，避免控件退化为无样式状态。
- 日间和暗黑模式必须使用 Element Plus CSS 变量或项目主题变量，不能写只适配白底的固定颜色。
- 表单保持简洁；同类标签、状态菜单、卡片、表格、弹窗和遮罩层沿用现有视觉语言。
- 普通弹窗和 MessageBox 在桌面、移动端均需视口居中并保留安全间距；内容过高时内部可滚动。
- 桌面表格列要对齐、间距平衡；移动端按现有断点自适应，不能依赖固定截图宽度。
- 所有保存和异步动作应有忙碌态、防重复提交、明确错误反馈和离开保护。
- 主题偏好存于浏览器；用户上传的截图保持原图，不因主题切换反色。
- 产品文案使用“交易计划”“关联订单”“计划事件”“计划复盘”“更新订单”等当前名称，避免恢复已废弃模块或旧标题。

## 10. 开发命令与验证边界

```powershell
npm install       # 安装依赖
npm run dev       # 同时启动 API 与 Vite
npm run build     # vue-tsc --noEmit + Vite 构建
npm start         # 运行已构建的本机服务
```

当前用户明确要求：不新增或运行测试，不做 API、OCR 试跑或浏览器验收。代码任务按需要可运行 `npm run build` 和 `git diff --check`；文档任务检查路径、内容和 diff 即可，无需构建。必须如实区分构建通过与运行时验证，未验证的行为要明确报告。

## 11. 修改、提交与推送流程

开始前：

1. 确认 `git rev-parse --show-toplevel` 是当前 `TradingRecord` 根目录。
2. 查看 `git status --short` 和本任务相关 diff，识别他人未提交改动。
3. 按本文件定位模块，只读取完成任务所需文件，不打开真实数据库或私人报告。

实施时：

- 只修改本任务负责的文件，不覆盖、不格式化、不顺带提交其他任务内容。
- 保持既有数据兼容和业务不变量；跨数据模型的改动先写清迁移、回退和冲突处理。
- 不用真实账户、订单、截图或报告作为源码示例；示例只用明显虚构数据。

完成后：

1. 查看精确 diff，运行允许的静态检查，并检查敏感信息。
2. 使用明确文件路径暂存，不使用 `git add .` 或 `git add -A`。
3. 仅提交本任务文件，报告提交号、检查结果和未验证风险。
4. 提交不等于推送。只有用户对当前任务明确要求远程推送时才可执行 push；不能沿用之前任务的授权。

## 12. 敏感数据与公开仓库规则

这是公开仓库。以下内容不得提交、上传、写入文档示例或 Git 历史：

- `data/`、`*.sqlite*`、`*.db*`，包括 WAL/SHM。
- 真实行情截图、订单截图、MT5 报告及导出的个人计划资料。
- `.env*`（`.env.example` 除外）、私钥、凭据、账户信息。
- 私人备份、旧仓库 `.git`、objects、refs 或包含敏感内容的历史。

`.gitignore` 是最后一道防线，不是脱敏替代品。暂存前检查 `git diff --cached`；公开文档截图只有在用户明确授权公开时才可纳入仓库。本 README 使用的主页截图已由用户明确要求展示。

## 13. 常见风险与修改前检查

- **状态覆盖**：计划正文保存和订单截图更新都必须避免覆盖数据库中的较新状态或字段。
- **订单归属**：订单号全局唯一，冲突时不得静默复制或转移到另一计划。
- **未知 OCR 值**：低置信度字段保持为空，不能用 `0`、当前值或推测值伪装识别成功。
- **布局误判**：挂单与持仓共享列结构，状态判定必须结合类型、时间和尾部信号；不要只凭列数判断。
- **事件原子性**：订单创建、状态、字段更新或锁定与事件写入必须同事务。
- **永久锁定**：锁定只允许已平仓订单且不可撤销；所有状态、手数、止损、止盈写入入口必须由后端拒绝锁定订单，不能只依赖前端禁用。
- **复盘并发**：保存必须校验 `expectedUpdatedAt`，冲突时提示重载。
- **图片边界**：行情截图可持久化；订单 OCR 截图不可持久化。两者不要混淆。
- **导出误解**：任何 UI 或文档都不得把 ZIP 描述成可恢复备份。
- **Element Plus 样式**：新增按需组件要同时引入对应样式；弹窗、日期控件、状态浮层等易受全局 CSS 影响。
- **数据迁移**：不得删表、删字段或用空库解决兼容问题；先备份、设计升级与回退，再动 schema。
- **离开保护**：修改页签、导航、弹窗或保存流程时，检查计划、订单草稿、复盘与行情吐槽未发布草稿；App 必须确认后再卸载当前视图。
- **时间语义**：应用 `createdAt/updatedAt` 使用 ISO 时间；MT5 报告时间保留报告字符串，不做时区转换。

动手前应先回答：此次改动属于哪个模型和文件？是否改变持久化结构？是否会覆盖已有数据？是否影响未保存内容、并发控制、导出、暗黑模式或移动端？若答案不明确，先从对应存储和编排组件确认现状。
