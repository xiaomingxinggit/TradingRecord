# 任务与交接

状态：todo → active → done；有具体阻碍用 blocked 并写原因。任务调度者负责此文件，Worker 返回结果而不并行改清单。

## 当前任务

### RESTRUCT-02：独立交易计划与订单

- 状态：done（实现、文档与构建完成；运行时验收由用户进行）；负责人 DEV。用户授权按新核心逻辑文档继续；优先于 EXEC-01 的内嵌入口及必选计划假设。
- 基线与归属：已提交 FLOW-01 f26fc5e 保留。接手保留 EXEC-01 的 13 个未提交文件：DEV 的任务/架构、OpeningPlans、PlanOrders、orders.ts；后端的 plans/plan-adjustments/export/index/plan-orders；OCR 的 price-ocr/order-ocr；过程 UI 的 PlanAdjustments。原 Worker 均已停止写入，本轮重新分区。已知 TS2345 行类型已局部修正，待最终集成构建。
- 目标：侧栏独立交易计划与交易订单；计划0..N订单，订单0..1计划；单账户ticket全局唯一，不自动转移。计划新增触发/失效条件，独立意图状态；订单拥有生命周期、过程/图片、成交资格复盘和关联计划对比。
- 历史兼容：原执行状态只保留为旧标记；不伪造订单；计划级调整/总结保留展示及导出，不按编号迁移。增量独立订单表，不DROP旧表、不改真实data，不激活旧review/practice。
- 订单：挂单目标价/手数/时间/有效期与实际成交量价时间分离；未成交可明确取消/失效，分批成交不自动拼单。截图盈利不当净盈亏，不猜币种/比例/R。保留阶段观察、幂等、事实冲突及明确来源绑定。
- 过程与复盘：新过程属于订单，支持独立调整/情绪/判断/安全图片；未关联订单也可记录。已确认成交才可写复盘草稿，已平仓才可完成，独立revision，不改订单/计划状态。旧simpleReview只读历史计划总结。
- 文件分区：独立后端 Worker 负责 server 除已完成OCR外全部新接口/存储/导出；订单前端 Worker 负责 PlanOrders、OrdersWorkspace、订单详情复盘与orders.ts；过程 Worker 负责新OrderProcess组件。DEV负责App/导航、OpeningPlans/计划状态、旧过程/总结兼容集成、文档及最终构建。共享接口按ARCHITECTURE新决策。
- 验证交付：禁止测试/OCR/API/浏览器试跑、启动服务或读写真实库；仅必要build、node静态和diff/引用核对。完成逐项映射检查后提交，不push，报告未运行验证内容。
- 实施映射（2026-09-18）：App / AppNavigation 两入口；OpeningPlans 保留原分析、截图、价位和 R，新增触发/失效条件、五种意图状态及派生执行进度。旧 executed 仅历史标记。PlanLinkedOrders 支持明确关联及跳转，订单新建不要求计划。
- 实施映射：OrdersWorkspace / PlanOrders 与独立 /api/orders 接通；票号字符串全局唯一、nullable planId、挂单/成交字段分离、手工取消/失效、直接持仓/平仓录入、同号冲突与观察幂等。未知开仓/平仓时间均可空，有值才校验；截图盈利不当净额。
- 实施映射：OrderProcess 记录独立价位/情绪/判断及每次最多4图；OrderReview 按实际成交资格保存、平仓后完成、独立revision。订单详情事实/计划对比/过程/复盘持续挂载，同ID事实更新不清其他草稿；首跳等待初始化，离开导航统一确认，菜单取消后保持实际页面高亮。
- 兼容与导出：旧计划过程和simpleReview在历史页只读展示，来源字段和原文保留；显式绑定仅建立来源关系，不复制条目。新增6张 tr_*_v1 订单表，未执行真实库初始化，无DROP/批量改写。全量ZIP为交易记录.zip，内含交易计划.md、交易订单.md及图片，未关联订单也导出；同一订单实体只输出一次，排除内部幂等字段。
- 文档：README、帮助和ARCHITECTURE统一改为最终独立实体模型，清除中间设计的互相冲突说明；升级前停服一致备份、旧版本无法完整读取新订单、ZIP不能恢复均已说明。旧报告文件导入仍不启用。
- 调度事实：本轮复用 flow_backend、flow_process_ui、order_ocr 三个独立 Worker，沿用明确请求 Astra/high、fork_turns=none 的分工；工具接受请求，实际模型无独立遥测。额度中断后继续已有成果，未新建用户任务。DEV负责入口、计划/旧历史集成、共享文档和提交，未重复派全量审查。
- 验证结果：RESTRUCT集成先后2次 npm run build（vue-tsc + Vite）通过；第二次覆盖帮助、导航高亮与来源显示的后续修改。保留既有 >500kB chunk 提示，最终主JS约658kB。Worker对11个后端模块执行node --check及其Vue组件静态编译通过；DEV补查两个OCR模块node --check通过，最终diff空白检查通过。
- 未验证：按用户要求未新增或运行测试、API/OCR试跑、浏览器验收、启动服务或访问真实库。SQL实际初始化、持久化/并发/重试、OCR布局与准确率、浏览器导航/附件/主题、ZIP阅读效果均未运行验证；构建通过不等于这些行为已实测。
- 交付：合并承接EXEC-01的本任务成果，统一独立Git提交，提交号见交付消息与Git历史；不推送。无剩余实现步骤。

### EXEC-01：同一订单的截图与生命周期

- 状态：由 RESTRUCT-02 承接并完成最终实现（不按被替代的旧范围独立提交）；负责人 DEV；基线 f26fc5e。原13文件保留复用；首次集成构建发现 TS2345 后暂停，后续最终模型与验证见上卡，准确率/运行时未验证。
- 目标：单账户、一份计划多订单、ticket 字符串全局唯一；同号订单的挂单 / 持仓 / 平仓截图补充同一档案及过程。不增加独立管理页，不启用旧复盘、模拟或报告导入模块。
- 数据边界：独立增量订单表与观察 / 请求去重；订单不可跨计划复制或自动转移。挂单类型与目标价区别于实际成交价；报告时钟原样规范化保存，应用录入时间为 UTC；未知价格、时间、盈利保留空，不猜币种 / 费用，不汇总金额。未声明取消不可推断取消，不支持分批平仓。
- 截图：明确挂单 / 持仓 / 已平仓模式；完整宽度表格与无表头行；识别只生成可逐行选择和纠正的草稿，确认后保存。票号不经 Number 转换，资金 balance 行排除，当前市价 / 浮盈不作平仓结果。原计划价位 OCR 保持。
- 过程：订单 ID 可用于价位 / 情绪，单订单不重复建组；旧组只能显式确认绑定当前计划订单，历史快照不改写。保留计划级情绪及各部分独立保存 / 草稿 / 在途保护。
- 冲突：全局归属、品种 / 方向 / 手数 / 已确认成交时间价格以及平仓结果冲突均显示差异，不静默覆盖；允许补全未知字段、明确前进阶段和新的 SL/TP 观察，不允许旧图退回状态。请求 ID 与截图观察身份幂等，内容相同的不同真实观察不能被永久历史去重误删。
- 实施顺序与所有权：DEV 写任务 / 架构、订单前端、计划页集成、依赖及最终构建；独立 Worker 分别负责订单后端 / 导出、订单 OCR、过程引用前端，各自文件不重叠。派工接口以架构决策与具体交接为准。
- 验收：订单列表 / 唯一订单数 / 已平仓数、逐行核对补录、同号补全与明确差异、阶段事件、旧组绑定及订单引用、三部分导出均有实际实现；数据兼容，日夜主题和草稿保护。
- 允许验证：必要 build / node --check / diff 与引用静态核对；禁止新增或运行测试、OCR/API 试跑、浏览器验收、启动服务或读取 / 修改真实数据库。样例只读查看，不将真实图片或样例内容入 Git。
- 交付：更新当前架构 / 使用说明 / 任务卡，独立提交 EXEC-01，不推送；如实记录 OCR 准确率、并发与浏览器交互等未运行验证内容。

### FLOW-01：单份记录的开仓—持仓—复盘

- 状态：done（实现、静态核对与构建完成，运行时验收由用户进行）；负责人 DEV。基线 3a2b392；接手时仅 ARCHITECTURE.md / TASKS.md 有 PM 编写的本任务设计，随后由 DEV 接管维护，无其他未提交功能代码。
- 目标：移除旧独立交易复盘与模拟练习的界面、API、实现和专用依赖；仅保留原计划入口，每份记录内部三部分：开仓计划 / 持仓过程 / 交易复盘。三部分共用 plan ID，不创建三份副本。
- 开仓计划：完整保留用户截图所示字段、截图、品种选择、价位 OCR、原计划收益风险比与保存/状态功能；原持仓调整移到第二部分。
- 持仓过程：沿用按持仓编号分组的价位调整历史与 OCR；增加情绪记录（平静、焦虑、恐惧、贪婪、急躁、其他，单选+选填备注），允许只记录情绪无需填价格。情绪记录属于当前计划，可选关联持仓组；用不同事件类型表达，不能以空价格覆盖上一次价位。
- 简单复盘：交易结果（未记录/未结束/盈利/亏损/持平），是否按计划执行（未评定/是/部分/否），做得好的地方，下次改进。支持保存草稿/完成复盘；完成至少写一项总结，结果可未记录；不计算盈亏/胜率，不引入金额、看板或报告导入。完成复盘不自动改变计划状态。
- 兼容：移除旧模块代码而非删除真实数据库表/数据；旧计划、截图、adjustmentJournal 仍可读写，原 MT5 来源快照保留。新流程只手动编号+OCR，不再依赖 MT5 导入/关联/绑定端点；已有来源组作为独立历史组可继续追加，不因取消旧关联表而被锁死，不自动合并同号组。
- 架构：三部分可用 Element Plus Tabs，切换统一保护全部未保存草稿与在途请求；正文、过程和复盘各自独立保存、服务端保护彼此字段。复盘版本冲突检查；追加保留幂等；旧记录缺字段视为空，不读写真实库试验。
- 导出：仅导出当前计划的三部分及截图，保留持仓调整来源和历史。不再导出独立旧复盘/模拟业务，不泄露内部幂等字段；旧数据库数据仍保留可另行恢复。
- 文件范围：src/App.vue、导航/帮助、OpeningPlans/PlanAdjustments 及新组件、server/store/index/plans/plan-adjustments/export 及新辅助模块、文档和依赖；删除旧 review/practice 前后端、样式、类型和专用导出，先核对实际依赖，保留 OCR 与 ZIP 所需依赖。不得改真实 data/，不引入新框架。
- 验收：单入口三部分完整可达；三类记录按同一计划隔离；情绪单独保存；复盘独立持久化；旧调整/来源不断链；移除模块无残留 import/API/UI 文案；日夜主题一致；导出三部分；切页/返回/关闭草稿保护；构建通过。
- 验证与交付：不运行测试/API/OCR/浏览器，不启动服务，不写真实库；必要 npm run build、node --check、diff/引用静态核对；提交本任务 Git，不推送。更新架构与任务卡并如实报告未运行时验证内容。
- 实施结果（2026-09-17）：唯一计划入口和同一 plan ID 的三个 Element Plus 页签已完成；原计划字段、截图、OCR、收益风险比、状态与排序保留。持仓过程提供独立价位 / 情绪表单，四项简单复盘支持草稿 / 完成；帮助与 README 已按当前产品重写。
- 兼容证据：计划 store 不再初始化旧业务 store；无 DROP、表重建或批量写库。旧组、entries、sourceSnapshot 保留，追加只在当前计划 journal 中定位组，不查已移除的关联服务。新组 manualTicket + UUID；emotions 数组与价格 entries 分开，旧缺失字段仅读时补空。
- 保存证据：正文 / 状态 / 过程 / 复盘均在事务中读取最新 payload 并仅合并自己负责的字段。simpleReview 独立 revision 不匹配返回 409，完成至少一项总结，计划状态不联动；过程追加沿用内容签名与请求 ID。导出白名单包括三部分和原始截图，排除内部 requests/signature/revision 与旧业务。
- 草稿证据：三个面板持续挂载；OpeningPlans 汇总三类 dirty/busy，页签保留草稿，返回 / 导航统一确认，关闭 / 刷新提示。过程价位 / 情绪分别持有重试编号并只清理成功的对应草稿；父级只接收保存时间，不把其他部分响应复制进输入。OCR 锁定目标，初始读取在 disabled 解除后补读，读取 / 写入 / OCR / 确认在途阻止切换。
- 移除范围：旧 ReviewWorkspace / ReviewDetail / ExecutionTable / LinkPositionsDialog / PlanReviewEntry、PracticeWorkspace / PracticeRecord / PracticeImages，reviews 类型/API/样式、practice 类型/样式，server review-store / review-routes / review-parser / practice / practice-export；去除 Cheerio、ExcelJS、专用 override 与无用日期 / 复选框 / 盈亏样式。OCR 引擎、PriceOcr 组件和 ZIP 依赖保留。
- 调度事实：两个独立 Worker 明确请求 gpt-6-astra / high、fork_turns=none，工具接受。首次因额度限制均失败、未落盘；2026-09-17 复用原 Worker 继续，分别负责 server/ 与 PlanAdjustments.vue，文件所有权不重叠。DEV 负责新复盘前端、三页签集成、入口清理、依赖、文档及一次构建；无重复审查派工。
- 验证结果：本轮一次 `npm run build`（vue-tsc + Vite）通过，保留既有超过 500 kB 的 bundle 提示。Worker 对全部现存 server/*.mjs 执行 `node --check` 通过；过程组件 Vue SFC 静态语法解析通过。`git diff --check` 通过；src/server/package 中旧导入、旧路由、绑定端点、reviewSnapshot/practiceSnapshot、旧依赖无残留。`npm install --package-lock-only --ignore-scripts --no-audit --no-fund` 同步锁文件成功。
- 未验证范围：未新增或运行测试、未 API/OCR 试跑、未浏览器验收、未启动服务，也未读写真实数据库。实际持久化、并发冲突、断网重试、OCR 准确率、离开提示、日夜布局和 ZIP 阅读效果仅静态核对，留给用户运行时验证；构建通过不等于这些行为已实测。
- 交付记录：本任务代码与文档统一 Git 提交，提交号见本次交付消息和 Git 历史；未推送。无剩余实现步骤。

### ADJ-01：按持仓记录止盈止损调整

- 历史说明：以下记录初版及补修时的事实；其中 MT5 新建 / 关联 / 绑定要求已由 FLOW-01 的手动独立组和历史快照兼容决策替代，不再作为当前实现要求。
- 状态：done（补修实现及约定的静态/构建检查完成，未做运行时验收）。负责人：DEV；保留初版提交，另增修复提交。
- 用户已确认：一个计划管理多笔持仓；在计划价位下面增加独立栏，通过图片识别/手动录入，按持仓编号分别保留调整链路，详情和 Markdown 导出均可见。
- 依赖：既有 OCR 已在初版 09f0438 纳入依赖，同一提交混入了 ADJ-01；890b481 仅补充初版入口。未按原建议拆成 OCR 前置提交，原负责人任务已归档，未获得额外完成确认。保留实际历史，不将此前构建通过当成需求已完成。
- 文件：本轮修改 TASKS.md、ARCHITECTURE.md、server/plan-adjustments.mjs、server/export.mjs、src/components/PlanAdjustments.vue、OpeningPlans.vue、PriceOcr.vue 及 src/plans.css。PriceOcr 仅扩展目标提示、活动状态、取消隔离及主题；未修改 README、依赖、OCR 引擎或 server/index.mjs。
- 设计依据：ARCHITECTURE.md 的 ADJ-01 三项决策。选择已有明确来源持仓，或手动建立独立记录组；手动组可显式绑定当前计划已关联的 MT5 持仓，不按编号猜测账户。
- 验收：不同持仓历史隔离；追加和重试不丢失/重复记录；普通计划编辑不改历史；保本止损可记录；OCR 填入正确目标；取消/错误/切换不误保存；已有记录和截图兼容；Element Plus 日夜主题；导出包含全部已保存调整和来源。
- 验证：不测试、不启动 API/浏览器验收；允许一次必要 npm run build 与静态差异检查，发现修改/失败才追加。不得对真实库执行迁移或试写。
- 交付：保留 09f0438 / 890b481，修复代码与本任务文档另提交，不推送；OCR 引擎及依赖不重写。
- 模型调度：向 DEV 请求 Luna/medium，要求为复杂实现派发 Astra/high 独立 Worker，返回简明交接；DEV 负责集成与验证。
- 初版实际调度：spawn_agent 接受 gpt-6-astra / high、fork_turns=none，Worker 仅落盘后端即被 DEV 中断；初版 UI 由 DEV 补写后错误报告完成。此次 PM 请求 Astra/high 直接补修；接收端无模型遥测，不自行声称运行参数已验证。
- 本轮修复范围：接入调整 OCR；可达的显式绑定；按操作内容保留重试 requestId；计划/调整两类草稿、在途/取消/切换/刷新保护；独立表单和保存；完整来源标签；按业务字段导出且排除内部请求历史。
- 静态证据：PlanAdjustments.applyOcr 只写当前调整 draft，捕获所选目标并在 OCR 打开时锁定目标；PriceOcr 沿用单行 apply 与多行选择，编号仍明确手填。绑定使用独立 selector/form 和请求，成功仅清理绑定选择、不清空价位草稿。
- 静态证据：appendAttempt / bindAttempt 各自按内容指纹持有 ID，失败与刷新历史不换 ID，成功或该操作内容改变才更新；重新读取发现服务器已保存时仍允许用旧 ID 确认结果。服务端事务/签名去重保留，并对已有来源组追加重新核对当前计划归属。
- 静态证据：OpeningPlans 通过 canLeave 汇总两类草稿，返回/导航/换计划均经过该入口；同计划详情/编辑/正文保存保留调整实例。beforeUnload 覆盖两类草稿及 OCR/写请求；请求在途、确认框和 OCR 打开时阻止切换。组件卸载/planId 变化会失效旧响应。三个保存表单彼此独立，submit 阻止冒泡；计划按钮明确归属正文 form。
- 静态证据：手动编号必填，来源标签含公司/服务器/账户/币种；Markdown 输出组 ID、原手动编号、建组/绑定时间和每条来源快照，空 SL/TP 标注“本次未设置”。用户文字使用 textBlock，adjustmentJournal 加入 knownFields，不重复输出内部 requests/signature。
- 验证结果：本轮 1 次 `npm run build`（vue-tsc + Vite）通过；`node --check server/plan-adjustments.mjs`、`node --check server/export.mjs`、`git diff --check` 通过。仍有既有 >500 kB bundle 提示。未运行测试、OCR/API 试跑、浏览器验收，未启动服务或读取/写入真实数据库。
- 尚未覆盖：真实图片单/多行识别准确率、浏览器离开确认实际表现、断网/超时重试及多窗口并发行为、导出文件实际阅读效果。以上只作静态代码路径核对，按用户要求留给手工运行时验收，不声称已实测。
- 提交记录：初版 09f0438、890b481 保留；本轮代码与文档以独立修复提交交付，提交号由 Git 历史及交付消息记录，不推送。

### WF-01：落实项目分工与交接

- 状态：done。负责人：PM；DEV 已完成接手核对，承担后续日常调度。
- 目标：用可执行的项目规范减少重复上下文和返工；保留单仓库与现有功能。
- 依赖：仓库整合完成，统一根目录基线 ef0f90a。
- 文件所有权：仅 AGENTS.md、ARCHITECTURE.md、TASKS.md。README、依赖及 OCR 代码均不属于本任务。
- 步骤：PM 建立规范与架构索引 → 显式向 DEV 请求 Luna/medium 执行一次限定范围核对 → PM 合并反馈、记录调度结果并提交文档。
- 验收：三个文档相互分工明确；代码路径有效；验证规则符合“不测试”；模型实际调度有工具结果；OCR 文件内容不变；Git 提交只含本任务文档。
- 验证：文档路径/内容核对、diff、受保护文件 SHA-256 比对；不运行产品、测试或构建。
- 发布：允许仅提交工作规范；本轮不推送远程。
- 调度记录：2026-09-16 向现有 DEV 显式传入 model=gpt-5.6-luna、thinking=medium，send_message_to_thread 成功接受，DEV 随后完成本次只读任务。工具回执未回显运行模型名称，DEV 自身也无法独立核实；记录参数已接受，不将自述当成模型遥测。PM 自身模型未通过工具切换。
- 完成证据：DEV 确认文档引用路径、npm scripts、数据边界与不测试规则一致；7 个受保护 OCR 文件 SHA-256 与任务开始时全部相同。本任务未运行测试、API、浏览器或产品构建；仅提交三份文档，不推送。

### OCR-EXISTING：现有图片识别工作

- 状态：done（既有成果已作为 ADJ-01 依赖集成，运行时识别未验收）。
- 交接事实：原 OCR 任务已归档，发送交接请求被工具拒绝，未获得新的完成确认。原 7 文件在 09f0438 中与初版调整功能一起提交；此前“仍未提交”的说明已失效。
- 集成范围：README.md、package.json、package-lock.json、server/index.mjs、server/price-ocr.mjs、src/components/OpeningPlans.vue、src/components/PriceOcr.vue。既有差异/哈希在本机归档中保留，私人路径不写入本清单。
- 后续：仅按明确任务维护。此次 ADJ-01 补修复用识别算法，仅增加组件兼容与目标隔离；没有重复实现 OCR，也未将构建通过当成识别准确率证据。

## 最近完成的基线

- 单仓库整合：已完成；公开历史与本地未提交工作统一在 TradingRecord。私人 Git 及原第二目录已归档，路径只记录在本机忽略说明中。
- 日间/暗黑主题：ef0f90a，已提交并推送。模拟表单简化：49e6ad2，已提交并推送。
- 当前文档任务不重做这些功能，也不把构建通过描述为产品运行时验收通过。

## 新任务卡模板

```text
ID / 状态 / 负责人：
目标与明确不包含的范围：
依赖任务 / 架构决策：
允许修改文件 / 其他任务占用文件：
输入和接口 / 数据兼容约束：
验收条件：
验证命令及允许范围 / 尚未覆盖的风险：
模型请求 / 工具实际调度结果：
下一步或阻碍：
完成证据 / 提交号 / 是否允许推送：
```

## Worker 返回格式

```text
任务 ID：
完成内容与文件：
验证：命令、结果（未运行须注明原因）
剩余问题与需要决策的点：
提交号或未提交说明：
```

先记录可复用的证据，再决定是否追加检查；不要求每个小任务附一篇报告。复杂任务拆分时，每张卡必须能独立说明接口与验收，避免向所有 Worker 复制完整聊天。
